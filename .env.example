# データソース設定
# 使用するデータソースのタイプ (API または CSV)
DATA_SOURCE_TYPE=CSV

# API設定（DATA_SOURCE_TYPE=API の場合）
API_ENDPOINT=https://zipcloud.ibsnet.co.jp/api

# CSV設定（DATA_SOURCE_TYPE=CSV の場合）
CSV_FILE_PATH=./data/postal-codes.csv

# タイムアウト設定（ミリ秒）
REQUEST_TIMEOUT=2000

# サーバー設定
PORT=3000
NODE_ENV=production
